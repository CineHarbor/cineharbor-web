# 2026-08-31 三源评分聚合 P0 落地 checkpoint

## 完成项

- 新增 `src/lib/ratings/`：
  - `types.ts` — 统一模型（ExternalIds / RatingEntry / RatingsBundle / ResolvedRating / RatingProvider 接口）。
  - `cache.ts` — 通用内存 TTL 缓存（与用户数据 IStorage 分离，超额即时驱逐）。
  - `title-normalize.ts` / `title-match.ts` — 标题归一 + 匹配策略/置信度。
  - `external-ids.ts` — 外部 ID 收集归一。
  - `providers/douban.ts`（真实接入，结构化 value+votes+url）、`providers/imdb.ts`（`IMDB_RATINGS_JSON` 门控）、`providers/rt.ts`（`RT_RATINGS_JSON` 门控，仅 Tomatometer）。
  - `resolver.ts` — 去重/限并发/共享缓存/单 provider 失败降级。
  - `client.ts` — 前端 `fetchRatingsBatch`。
- 新增 `POST /api/ratings/batch`（鉴权 + 校验 + 按 SiteConfig 开关选 provider + 私有缓存头）。
- `SiteConfig` 增加 `ShowDoubanRating / ShowImdbRating / ShowRtRating`（`admin.types.ts` + `config.ts` 默认全开）。
- `LegacySearchPage` 切到统一接口（原有豆瓣角标零回退），`VideoCard` 增加评分行（豆瓣/IMDb/RT，仅显示有值源；RT 显示 `xx%`）。
- README 补充环境变量与开关说明。

## 验证

- `pnpm typecheck` 通过。
- `pnpm test` 全量通过（108 suites / 517 tests，含新增 ratings 单测 24 例）。
- `pnpm build` 通过（修正 no-useless-escape 与 no-console/import-sort 后）。

## 遗留（见 next-actions）

- P1：IMDb 真实数据源 transport（官方 datasets 同步脚本 / 官方 API key）、`douban_id → imdb_id` 反查。
- P2：RT 授权 feed transport、播放详情页完整评分区。
- P3：手工绑定/异常回收/命中率统计/同步脚本测试。